export enum Routes {
    Categories = '/categories',
    Expenses = '/expenses',
    Balance = '/balance',
    Login = '/login',
    Register = '/register'
}

export const HIDDEN_HEADER_PATHS = ['/login', '/register']